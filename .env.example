# ==============================================================================
# Environment Variables Configuration
# ==============================================================================
# Copy this file to .env.local for local development
# For Vercel deployment, add these variables in the Vercel dashboard
# ==============================================================================

# ==============================================================================
# PUBLIC VARIABLES (Safe to expose - available in browser)
# These are prefixed with NEXT_PUBLIC_ and can be used in client-side code
# ==============================================================================

# Supabase URL - Your Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase Anonymous Key - Safe for client-side use (RLS enforced)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Site URL for email links and redirects
NEXT_PUBLIC_SITE_URL=https://your-site.com

# ==============================================================================
# SERVER-SIDE ONLY VARIABLES (Never exposed to browser)
# These are NOT prefixed with NEXT_PUBLIC_ and only available in API routes
# ==============================================================================

# Supabase Service Role Key - DANGER: Bypasses Row Level Security!
# Only used in /api/* routes for admin operations
# NEVER prefix with NEXT_PUBLIC_ as this would expose it to the browser
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Admin email for notifications when new tools are submitted
# ADMIN_NOTIFICATION_EMAIL=admin@example.com

# GitHub Token for increased API rate limits (optional)
# GITHUB_TOKEN=your-github-token
