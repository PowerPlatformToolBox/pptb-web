# Supabase Configuration (Optional)
# These are only needed if you want to add backend features like analytics or user accounts

NEXT_PUBLIC_SUPABASE_URL=your-project-url.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase Service Role Key (Required for server-side operations like tool intake)
# WARNING: Never expose this key on the client side!
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Admin Notification Settings (Optional)
# Email address to receive notifications when new tools are submitted
# ADMIN_NOTIFICATION_EMAIL=admin@example.com

# Site URL for email links (Optional)
# NEXT_PUBLIC_SITE_URL=https://your-site.com

# GitHub Token (Optional)
# Add a GitHub personal access token to increase API rate limits
# GITHUB_TOKEN=your-github-token
